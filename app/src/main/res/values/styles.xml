<resources>

    <!-- Base application theme. -->
    <style name="Theme.Design" parent="Base.Theme.Design">
    </style>

    <style name="Base.Theme.Design" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="favorite" parent="Widget.AppCompat.Light.ActionButton">
        <item name="android:src">@drawable/button_state</item>
    </style>

</resources>
